source 'https://rubygems.org'

gem 'sinatra', require: 'sinatra/base'
gem 'sqlite3'
gem 'activerecord'
gem 'sinatra-activerecord'

# use to seperate dependencies for diff env
group :development, :test do
  gem 'shotgun'
  gem 'minitest'
  gem 'capybara'

  # lets you view sqlite db
  gem 'tux'
end
